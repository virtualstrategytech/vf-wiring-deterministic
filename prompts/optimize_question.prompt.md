Goal: make the user's message short, specific, and teachable.
Input: {user_message}

Rules:

- Keep to 1–2 sentences.
- Preserve user intent and domain terms.
- Remove fluff; keep key nouns, verbs, metrics, timeframe.
- If ambiguous, include the single most useful clarifier as a bracketed hint.

Return: the optimized question ONLY (no preface).
